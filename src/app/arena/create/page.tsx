import { CreateArenaForm } from '@/features/arena/components/create-arena-form';

export default function CreateArenaPage() {
    return (
        <div className="min-h-screen bg-gradient-to-br from-gray-50 to-gray-100 py-12 px-4">
            <CreateArenaForm />
        </div>
    );
}
